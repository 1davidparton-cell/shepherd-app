import ScriptureReader from '../../components/ScriptureReader';

export default function AdminScripture() {
  return (
    <div style={{ position: 'relative', flex: 1, minHeight: 0 }}>
      <ScriptureReader />
    </div>
  );
}
